apiVersion: v1
kind: ConfigMap
metadata:
  name: pgadmin-servers
  namespace: data
data:
  servers.json: |
    {
      "Servers": {
        "1": {
          "Name": "Postgres (job-info-collector)",
          "Group": "Mac mini lab",
          "Host": "postgres-rw.data.svc.cluster.local",
          "Port": 5432,
          "MaintenanceDB": "job-info-collector",
          "Username": "job-info-collector",
          "SSLMode": "prefer"
        }
      }
    }
